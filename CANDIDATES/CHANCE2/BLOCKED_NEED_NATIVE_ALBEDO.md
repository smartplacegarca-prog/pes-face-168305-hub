# CHANCE 2 — BLOCKED

`CHANCE2 = AUTHORIZED` but the only legal source file is missing from this environment.

Required starting albedo:
`face_bsm_alp.ftex` SHA-256 `6edb1b440a334915b7731f3158cd2f1ed731bbd5ca529145b9136077f1b6e352`

Searched: sandbox `/tmp` + `/workspace` + GitHub hub (all releases/branches). **Zero copies.**

Cannot start from V3.2 / 5603a / V4.x FTEX. That would be a second variable and a forbidden base.

## Send
1. `face_bsm_alp.ftex` with that exact SHA-256 (from NATIVE_CLONE_CONTROL).
2. Optional: full `NATIVE_CLONE_CONTROL` zip so the reversible kit can ship the frozen companions for hash proof.

`READY_FOR_GAME = NO`
