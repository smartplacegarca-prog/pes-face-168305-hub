# ARTIFACTS

O repositório guarda metadados e hashes dos binários grandes.

Binários pesados como ZIP/FMDL/FTEX podem ficar fora do Git normal, em Release, Git LFS ou storage compartilhado.

Nunca substitua um artefato mantendo o mesmo nome sem atualizar o SHA-256 em `REGISTRY.json`.
